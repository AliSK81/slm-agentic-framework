# Examples

This directory contains minimal usage examples for the framework.

- `minimal_session.py`: runs a small end-to-end session for a simple coding task

Before running examples:

1. Create and activate a virtual environment
2. Install dependencies and package (`pip install -r requirements.txt && pip install -e .`)
3. Copy `.env.example` to `.env` and configure your provider
